<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android" xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main_activity_navigation"
    app:startDestination="@id/cameraFragment">

    <fragment
        android:id="@+id/cameraFragment"
        android:name="com.spx.muticamera.CameraFragment"
        android:label="CameraFragment" >
        <action
            android:id="@+id/action_record_to_play"
            app:destination="@id/playRecordingFragment" />
    </fragment>
    <fragment
        android:id="@+id/playRecordingFragment"
        android:name="com.spx.muticamera.play.PlayRecordingFragment"
        android:label="playRecordingFragment" />
</navigation>